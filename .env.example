# Discourse Configuration Template
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env to git - it contains sensitive information

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# Domain Configuration
DISCOURSE_HOSTNAME=discourse.example.com

# Administrator Setup
DISCOURSE_DEVELOPER_EMAILS=admin@example.com,dev@example.com

# SMTP Configuration (Required for email functionality)
DISCOURSE_SMTP_ADDRESS=smtp.example.com
DISCOURSE_SMTP_PORT=587
DISCOURSE_SMTP_USER_NAME=user@example.com
DISCOURSE_SMTP_PASSWORD=your_smtp_password_here
DISCOURSE_SMTP_ENABLE_START_TLS=true

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# SSL/TLS Configuration (uncomment if using Let's Encrypt)
#LETSENCRYPT_ACCOUNT_EMAIL=admin@example.com

# SMTP Advanced Settings
#DISCOURSE_SMTP_DOMAIN=discourse.example.com
#DISCOURSE_NOTIFICATION_EMAIL=noreply@discourse.example.com
#DISCOURSE_SMTP_OPENSSL_VERIFY_MODE=peer
#DISCOURSE_SMTP_AUTHENTICATION=plain

# Database Configuration (usually auto-generated)
#DISCOURSE_DB_PASSWORD=auto_generated_password

# CDN Configuration
#DISCOURSE_CDN_URL=https://cdn.example.com

# MaxMind GeoLocation (for IP lookups)
#DISCOURSE_MAXMIND_ACCOUNT_ID=123456
#DISCOURSE_MAXMIND_LICENSE_KEY=your_maxmind_key_here

# Performance Settings
#UNICORN_WORKERS=4
#DISCOURSE_DB_SHARED_BUFFERS=256MB

# =============================================================================
# DEVELOPMENT SETTINGS
# =============================================================================

# Uncomment for development/testing
#DISCOURSE_FORCE_HTTPS=false
#DISCOURSE_DEVELOPER_EMAILS=dev@localhost
#DISCOURSE_HOSTNAME=localhost

# =============================================================================
# AWS SECRETS MANAGER CONFIGURATION
# =============================================================================
# These settings control the AWS Secrets Manager integration
# Leave commented for local development

#AWS_DEFAULT_REGION=us-east-1
#AWS_SECRET_NAME=discourse/production

# =============================================================================
# NOTES
# =============================================================================
# 
# 1. This file serves as a template. Copy it to .env and customize.
# 2. The load_secrets.sh script will try AWS Secrets Manager first, 
#    then fall back to this .env file.
# 3. In production, use AWS Secrets Manager. Use .env only for development.
# 4. Never commit .env files to version control.
# 5. Use strong, unique passwords for all services.
#
